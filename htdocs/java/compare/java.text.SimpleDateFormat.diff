--- /home/tromey/gnu/Nightly/classpath/classpath/java/text/SimpleDateFormat.java	2003-11-29 02:20:16.000000000 -0700
+++ java/text/SimpleDateFormat.java	2003-11-12 02:18:14.000000000 -0700
@@ -117,8 +117,8 @@
       field = formatData.getLocalPatternChars().indexOf(thisChar);
       if (field == -1) {
 	current = null;
-	if ((thisChar >= 'A' && thisChar <= 'Z')
-	    || (thisChar >= 'a' && thisChar <= 'z')) {
+	if (Character.isLowerCase (thisChar)
+	    || Character.isUpperCase (thisChar)) {
 	  // Not a valid letter
 	  tokens.add(new FieldSizePair(-1,0));
 	} else if (thisChar == '\'') {
