--- /home/tromey/gnu/Nightly/classpath/classpath/gnu/java/nio/ServerSocketChannelImpl.java	2003-06-16 02:23:37.000000000 -0600
+++ gnu/java/nio/ServerSocketChannelImpl.java	2003-06-15 02:21:55.000000000 -0600
@@ -47,18 +47,12 @@
 
 class ServerSocketChannelImpl extends ServerSocketChannel
 {
-  ServerSocket sock_object;
+  ServerSocket serverSocket;
   int fd;
-  int local_port;
+//   int local_port;
   boolean blocking = true;
   boolean connected = false;
-  InetSocketAddress sa;
-
-  private static int NioSocketAccept (ServerSocketChannelImpl server, 
-                                      SocketChannelImpl s)
-  {
-    return 0;
-  }
+//   InetSocketAddress sa;
 
   protected ServerSocketChannelImpl (SelectorProvider provider)
     throws IOException
@@ -68,7 +62,7 @@
 
     try
       {
-        sock_object = new ServerSocket ();
+        serverSocket = new ServerSocket ();
       }
     catch (IOException e)
       {
@@ -106,12 +100,12 @@
   {
     SocketChannelImpl result = new SocketChannelImpl (provider ());
     result.sa = new InetSocketAddress (0);
-    int res = NioSocketAccept (this, result);
+    //int res = SocketAccept (this,result);
     return result;
   }
 
   public ServerSocket socket ()
   {
-    return sock_object;
+    return serverSocket;
   }
 }
