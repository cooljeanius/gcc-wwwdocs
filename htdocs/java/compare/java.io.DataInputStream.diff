--- /home/tromey/gnu/Nightly/classpath/classpath/java/io/DataInputStream.java	2004-04-13 02:20:21.000000000 -0600
+++ java/io/DataInputStream.java	2003-06-13 02:27:33.000000000 -0600
@@ -277,16 +277,16 @@
    * buffer
    * @exception IOException If any other error occurs
    */
-  public final void readFully (byte[] buf, int offset, int len) throws IOException
+  public final void readFully (byte[] b, int off, int len) throws IOException
   {
     while (len > 0)
       {
 	// in.read will block until some data is available.
-	int numread = in.read (buf, offset, len);
+	int numread = in.read (b, off, len);
 	if (numread < 0)
 	  throw new EOFException ();
 	len -= numread;
-	offset += numread;
+	off += numread;
       }
   }
 
