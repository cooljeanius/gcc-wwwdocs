--- /home/tromey/gnu/Nightly/classpath/classpath/java/util/Collections.java	2004-09-22 02:30:01.000000000 -0600
+++ java/util/Collections.java	2004-09-27 02:15:43.000000000 -0600
@@ -1713,11 +1713,11 @@
   {
     Object[] a = l.toArray();
     Arrays.sort(a, c);
-    ListIterator i = l.listIterator(a.length);
-    for (int pos = a.length; --pos >= 0; )
+    ListIterator i = l.listIterator();
+    for (int pos = 0, alen = a.length;  pos < alen;  pos++)
       {
-	i.previous();
-	i.set(a[pos]);
+        i.next();
+        i.set(a[pos]);
       }
   }
 
