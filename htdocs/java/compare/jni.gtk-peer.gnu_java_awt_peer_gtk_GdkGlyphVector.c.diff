--- /home/tromey/gnu/Nightly/classpath/classpath/native/jni/gtk-peer/gnu_java_awt_peer_gtk_GdkGlyphVector.c	2004-03-29 02:24:15.000000000 -0700
+++ jni/gtk-peer/gnu_java_awt_peer_gtk_GdkGlyphVector.c	2004-05-27 02:19:09.000000000 -0600
@@ -636,9 +636,12 @@
 
   gdk_threads_leave ();
 
-  return 
+  return 1;
+  /* FIXME: Pango doesn't seem to have decided how it will deal
+     with vertical text. for the time being we inherit this limitation.
     ((dir == PANGO_DIRECTION_LTR) ||
      (dir == PANGO_DIRECTION_RTL));    
+  */
 }
 
 
