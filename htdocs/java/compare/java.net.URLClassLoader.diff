--- /home/tromey/gnu/Nightly/classpath/classpath/java/net/URLClassLoader.java	2003-07-18 02:18:37.000000000 -0600
+++ java/net/URLClassLoader.java	2003-06-20 02:21:15.000000000 -0600
@@ -311,9 +311,6 @@
       if (jarfile == null)
 	return null;
 
-      if (name.startsWith("/"))
-        name = name.substring(1);
-
       JarEntry je = jarfile.getJarEntry(name);
       if(je != null)
 	return new JarURLResource(this, name, je);
@@ -657,11 +654,6 @@
    */
   protected void addURL(URL newUrl)
   {
-    addURLImpl(newUrl);
-  }
-
-  private void addURLImpl(URL newUrl)
-  {
     synchronized(urlloaders)
       {
 	if (newUrl == null)
@@ -698,7 +690,7 @@
   {
     for (int i = 0; i < newUrls.length; i++)
     {
-      addURLImpl(newUrls[i]);
+      addURL(newUrls[i]);
     }
   }
 
