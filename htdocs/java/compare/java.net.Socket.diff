--- /home/tromey/gnu/Nightly/classpath/classpath/java/net/Socket.java	2003-06-19 02:34:45.000000000 -0600
+++ java/net/Socket.java	2003-06-28 02:18:20.000000000 -0600
@@ -305,6 +305,15 @@
     // that default.  JDK 1.2 doc infers not to do a bind.
   }
 
+  /*
+   * This method may only be used by java.nio.channels.ServerSocketChannel.accept and
+   * java.nio.channels.SocketChannel.open.
+   */
+  void setChannel (SocketChannel ch)
+  {
+    this.ch = ch;
+  }
+
   /**
    * Binds the socket to the givent local address/port
    *
