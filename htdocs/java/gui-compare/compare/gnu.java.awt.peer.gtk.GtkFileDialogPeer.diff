--- /home/tromey/gnu/Nightly/classpath/classpath/gnu/java/awt/peer/gtk/GtkFileDialogPeer.java	2004-07-23 02:23:49.000000000 -0600
+++ gnu/java/awt/peer/gtk/GtkFileDialogPeer.java	2004-07-22 11:06:19.000000000 -0600
@@ -99,7 +99,7 @@
 
     // GtkFileChooser requires absolute filenames. If the given filename
     // is not absolute, let's construct it based on current directory.
-        currentFile = fileName;
+    currentFile = fileName;
     if (fileName.indexOf(FS) == 0)
       {
         nativeSetFile (fileName);
